# 运行时依赖
openai==2.43.0          # DeepSeek 用 OpenAI 兼容 SDK（base_url 指向 api.deepseek.com）
pydantic==2.13.4        # 内部消息格式校验
python-dotenv==1.2.2    # 读取 .env

# 开发/测试依赖
pytest==9.1.1
pytest-mock==3.15.1
